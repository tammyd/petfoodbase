{% extends "page_template.html.twig" %}

{% block pageContainerClasses%}  container container-post blog {% endblock %}


{% block mainContent %}

  <div class="row">

    <!-- Blog Post Content Column -->
    <div class="col-lg-12">

      <!-- Blog Post -->
      <!-- Title -->
      {% if hideTitle is not defined %}
        <h1 >{{ post.title | raw}}</h1>
      {% else %}
        {% if not hideTitle %}
          <h1 >{{ post.title | raw}}</h1>
        {% endif %}
      {% endif %}
      
      

      

      <!-- Preview Image -->
      {#{% if post.image %}#}
        {#{% set imgClass = "" %}#}
        {#{% if post.meta.image_well %}#}
          {#{% set imgClass = 'pseudo-well' %}#}
        {#{% endif %}#}

        {#<img class="img-responsive {{ imgClass }}" src="{{ post.image }}" alt="{{ post.title }}">#}

        {#<hr>#}
      {#{% endif %}#}

      <!-- Post Content -->
      {{ post.html | raw }}
      <hr>


    </div>
  </div>



{% endblock %}

{% block support %}
{% endblock %}

